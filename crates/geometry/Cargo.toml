[package]
name = "geometry"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[dependencies]
fontdb = "0.22"
i_overlay = "7.0"
serde = { version = "1", features = ["derive"] }
ttf-parser = "0.24"
