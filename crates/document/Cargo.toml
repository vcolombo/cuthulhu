[package]
name = "document"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[dependencies]
geometry = { path = "../geometry" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
