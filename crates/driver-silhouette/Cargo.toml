[package]
name = "driver-silhouette"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[dependencies]
driver-core = { path = "../driver-core" }
geometry = { path = "../geometry" }
nusb = "0.1"
