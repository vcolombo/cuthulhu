# SP3 manual checklist (per release)
- [ ] Switch machine Cameo 5 ⇄ Puma IV — artboard resizes.
- [ ] Import a complex SVG (import is now available via the TopBar Import button) — stays 60fps while panning/zooming (deferred — SP3.1: canvas pan/zoom not yet implemented).
- [ ] Drag/scale/rotate — drag works via the canvas; scale works via the properties panel W/H fields; rotate and on-canvas handles (deferred — SP3.1); one undo reverts the whole gesture.
- [ ] Boolean union/subtract/intersect/exclude on two overlapping shapes — correct result.
- [ ] Add text in a system font — converts to a cut path on save.
- [ ] Dark and light themes both legible; tabular numerals align in the properties panel (deferred — SP3.1: dark-only Workbench theme shipped).
